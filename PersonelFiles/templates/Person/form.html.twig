{% extends 'base.html.twig' %}

{% block body %}

<div class="form-control">

    <h1>Add Person</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Submit</button>
    {{ form_end(form) }}

</div>

{% endblock %}
